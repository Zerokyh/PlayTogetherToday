import { Box } from "@mui/material";
import { WidthHalfBoxSize } from "../../styles/mui";

const WidthHalfBox = () => {
  return (
    <Box
      sx={{
        ...WidthHalfBoxSize,
      }}
    ></Box>
  );
};

export default WidthHalfBox;
